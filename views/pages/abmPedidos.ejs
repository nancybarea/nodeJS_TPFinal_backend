<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container mt-3">
        <h2>ABM de Pedidos</h2>
        <p>Listado de pedidos (carritos que se solicitó la compra) realizadas por los usuarios de Argentina Aventura   </p>
        <% const hayProductos=pedidosList.length> 0; %>
        <div class="table-responsive">
            <% if(hayProductos){ %>                
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id Pedido</th>
                            <th>email</th>
                            <th>estado</th>
                            <th>Fecha de compra</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                    <% for (const item of pedidosList) {%>
                        <tr>
                            <td><%= item.id %></td>
                            <td><%= item.email %></td>
                            <td><%= item.estado %></td>
                            <td><%= item.fechaPedida %></td>
                            <td>
                                <a href="#">modificar</a>
                                &nbsp;&nbsp;<a href="#">Ver Productos</a>
                                &nbsp;&nbsp;<a href="#">borrar</a>
                            </td>
                        </tr>
                    <%};%>
                    </tbody>
                </table>
            <% } else{ %>
                <h3 class="alert alert-danger">No hay pedidos dados de alta</h1>
            <% } %>
        </div>
        <button type="button" class="btn btn-dark">Nuevo Pedido</button>
    </div>
</body>

</html>