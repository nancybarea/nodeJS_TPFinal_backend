<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container mt-3">
        <h2>ABM de Carritos</h2>
        <p>Listado de carritos de todos los usuarios en Argentina Aventura   </p>
        <% const hayProductos=carritosList.length> 0; %>
        <div class="table-responsive">
            <% if(hayProductos){ %>                
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id Carrito</th>
                            <th>Usuario</th>
                            <th>estado</th>
                            <th>Fecha Ult. Modif.</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                    <% for (const item of carritosList) {%>
                        <tr>
                            <td><%= item.id %></td>
                            <td><%= item.emailUsuario %></td>
                            <td><%= item.estado %></td>
                            <td><%= item.fechaUltModif %></td>
                            <td>
                                <a href="#">modificar</a>
                                &nbsp;&nbsp;<a href="#">Ver Productos</a>
                                &nbsp;&nbsp;<a href="#">borrar</a>
                            </td>
                        </tr>
                    <%};%>
                    </tbody>
                </table>
            <% } else{ %>
                <h3 class="alert alert-danger">No hay carritos dados de alta</h1>
            <% } %>
        </div>
        <button type="button" class="btn btn-dark">Nuevo Carrito</button>
    </div>
</body>

</html>