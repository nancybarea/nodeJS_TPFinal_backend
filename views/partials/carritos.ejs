<% const hayCarritos=carrito.length> 0; %>
    <h3>Listado del carrito</h3>
    <div class="container">
        <% if(hayCarritos){ %>
            <div>
                <table>
                    <tr>
                        <th>Id Product</th>
                        <th>Nombre</th>
                        <th>Precio</th>
                    </tr>
                    <% for (const item of carrito) {%>
                        <tr>
                            <td><%= item.id %></td>
                            <td><%= item.estado %></td>
                            <td><%= item.productos %></td>
                        </tr>
                    <%};%>
                </table>
            </div>
        <% } else{ %>
            <h3 class="alert alert-danger">No hay carritos dados de alta</h1>
        <% } %>
    </div>
